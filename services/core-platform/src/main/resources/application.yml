spring:
  application:
    name: core-platform

tongfuli:
  orchestrator:
    base-url: http://127.0.0.1:8001

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8080
